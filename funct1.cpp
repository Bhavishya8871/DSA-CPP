#include<iostream>
using namespace std;

int addsum(int n,int m){
    int sum=n+m;
    cout<<sum;
}

int main(){
    addsum(18,10);
    addsum(18,1);   
    addsum(18,82);
    addsum(18,32);
    return 0;
}